# How to setup Dotfiles on a system basis
- only supports NixOS and Nix-Darwin (MacOS) at the moment

## NixOS 
- obtain a live [iso](https://nixos.org/download)
- run the installer
### Get the repo
- install Git on your new system
`export NIX_CONFIG="experimental-features = nix-command flakes"`

- get the template:
`
